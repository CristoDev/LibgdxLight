en cours
- gestion de la direction en fonction des touches
    -> avoir une pile des touches appuyées
        -> direction = dernière touche appuyée
        -> linked list? peekLast
        https://docs.oracle.com/javase/9/docs/api/java/util/LinkedList.html
        -> pb avec la dernière touche, quand on n'appuie plus, ça reste dans la même direction si on a 2 touches appuyées
            -> exemple:
                * gauche puis haut : animation vers le haut (OK)
                * lacher haut: animation vers le haut toujours (KO)

à faire:

- github
https://github.com/CristoDev/LibgdxLight/issues


-gestion des points spécifiques sur la carte:
    - téléporteur (départ - arrivée)
    - entrée grotte
    - entrée maison
    --> utiliser un layer qui ne sera pas directement intégré sur la map
        --> gestion de points (ou zones) uniquement avec une action indiquée dans le fichier TMX

- asset manager
    -> equipement
        -> inventaire
    -> lightPlayerGraphics et/ou lightGraphics
        -> prendre en compte la direction (angle ou autre)

- mettre en place un observer?
    exemple: téléportation:
                - lancer une animation (départ)
                - jouer un son
                - déplacer le personnage
                - lancer une animation (arrivée)
                - jouer un son


- utilisation du clic gauche -> envoyer une action suivant le type d'objet rencontré
    - ne pas prendre en compte l'arme dans un premier temps
